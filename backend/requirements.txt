fastapi==0.115.12
uvicorn[standard]==0.34.2
sqlalchemy==2.0.41
alembic==1.15.2
psycopg2-binary==2.9.10
pydantic==2.11.3
pydantic-settings==2.9.1
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20
httpx==0.28.1
redis==6.2.0
anthropic==0.52.0
boto3==1.38.24
elasticsearch==8.18.0
stripe==12.2.0
resend==2.7.0
pusher==3.3.2
python-dotenv==1.1.0
celery==5.5.2
sentry-sdk[fastapi]==2.19.2
pytest==8.4.0
pytest-asyncio==1.0.0
pytest-cov==6.1.1
cryptography==44.0.2
pyotp==2.9.0
qrcode[pil]==8.0

# --- P-00: dependencies the code already imports but never declared ---
email-validator==2.2.0      # pydantic EmailStr - without it pytest cannot collect
reportlab==4.2.5            # services/backbone/pdf_export.py
PyJWT==2.10.1               # tests/security/test_rate_limiting.py

# --- P-00: pinned to keep passlib working ---
# passlib 1.7.4 reads bcrypt.__about__, removed in bcrypt 5. Unpinned, this
# breaks every password hash with "password cannot be longer than 72 bytes".
bcrypt==4.0.1

# --- P-00: landed up front so no later package edits this file ---
tenacity==9.0.0             # P-07 retry/backoff for partner integrations
